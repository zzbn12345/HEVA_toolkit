# Incomplete dummy HEVA project

Open this folder with **Open existing project** in the HEVA app, then run validation.

The single registered document deliberately has a missing source and no completed metadata or
annotations. These are teaching errors: the validation report should identify them and direct
the user to the relevant record. Do not use this project as a valid Data Package.

The finished distribution shape is in `examples/dummy-data-package/`.
