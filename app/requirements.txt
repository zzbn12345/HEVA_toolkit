# The application depends on the lightweight management contract, not the NLP stack.
-r ../requirements.txt

fastapi>=0.116,<1.0
httpx>=0.28,<1.0
python-multipart>=0.0.20,<1.0
uvicorn>=0.35,<1.0
pytest>=8.0,<9.0
